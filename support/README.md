# support/ — 遗留目录

> **状态**：遗留（legacy）。本目录内容为临时生成的辅助文件，不作为正式知识资产。
>
> `support/` 已在 `.gitignore` 中忽略，不纳入版本控制。

## 现有文件

| 文件 | 说明 |
|------|------|
| `ascii-diagrams-extracted.md` | 从 `knowledge/` 提取的 ASCII 代码图汇总（临时产物） |
