# 支撑资产

这里统一放仓库中的支撑性方法资产。

当前分为三类：

- `support/docs/`：给用户看的说明文档、操作手册、checklists
- `support/templates/`：长期模板与表格骨架
- `support/prompts/`：面向 AI 协作的提示词

唯一保持顶层独立的相关目录是：

- `skills/`：因为它通常需要保持固定目录结构，方便被客户端直接加载
