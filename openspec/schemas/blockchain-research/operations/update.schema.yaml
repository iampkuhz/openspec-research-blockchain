name: blockchain-research.operation.update
description: "用于更新已有 Knowledge artifact。"

x_applies_when:
  change_operation: update

x_requires:
  existing_targets: true
  update_summary: true
  traceability: true

x_validators:
  - publish_targets
  - traceability
