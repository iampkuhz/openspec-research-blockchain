name: blockchain-research.operation.create
description: "用于创建新的 Knowledge artifact。"

x_applies_when:
  change_operation: create

x_requires:
  publish_target_status: new

x_validators:
  - publish_targets
