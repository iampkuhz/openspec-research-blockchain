> 状态：示范性依赖地图。用于展示上层研究如何声明依赖而不是复写全文。

# 依赖地图

## 依赖范围

本研究的目标不是重写底层 EIP 全文，而是建立“问题层与关系层”的解释框架。因此依赖的重点是抽取对象定位与边界，而不是复制流程细节。

## 依赖表

| 依赖对象 | 层级 | 预算 | 抽取内容 | 为什么这个深度足够 | 不重复什么 |
| --- | --- | --- | --- | --- | --- |
| `knowledge/analysis/primitives/eip-4337/` | `primitive` | `deep` | `UserOperation`、`EntryPoint`、生态分层边界 | 4337 是当前框架的主锚点，需要机制级理解 | 不复制完整术语定义与全文流程 |
| `EIP-7702` | `primitive` | `focused` | 它想解决的目标问题与授权边界 | 当前只需要定位其问题层 | 不复制具体字段或完整规范 |
| `EIP-3074` | `primitive` | `focused` | delegation 相关目标与边界 | 当前主要用于比较关系 | 不复制完整机制细节 |

## 复用说明

- 在 `analysis.md` 中，优先复用“问题层定位”与“边界说明”。
- 在 `verdict.md` 中，只复用那些已经足以支持框架结论的点。

## 缺口

- `EIP-7702` 和 `EIP-3074` 尚未在本仓库形成完整底层 case，后续如果上层判断增多，应优先补齐对应 `primitive`。
